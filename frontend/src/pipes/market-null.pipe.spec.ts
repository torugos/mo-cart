import { MarketNullPipe } from './market-null.pipe';

describe('MarketNullPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketNullPipe();
    expect(pipe).toBeTruthy();
  });
});
